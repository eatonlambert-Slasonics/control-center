# Virtual environments
.venv/
venv/
env/

# Python compiled cache
__pycache__/
*.pyc

# App logs
logs/

# Live project config -- persisted/mutated at runtime by the Add Project/Repo/
# Service UI. See projects.json.example for the seed/schema.
projects.json

# Per-action step-tracker state (see "action" steps in app.py) -- local
# dashboard progress, not project/repo config, recreated on first use.
action_progress.json
